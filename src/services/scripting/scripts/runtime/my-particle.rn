pub fn skip() {
    #{ "nickname": cyb::context.input.nickname, "action": "skip" }
}

pub fn answer(content) {
    #{ "nickname": cyb::context.input.nickname, "action": "answer", "content": content }
}


pub async fn main() {
    let my_particle_ctx = cyb::context.myParticle;
    my_particle(my_particle_ctx.nickname, my_particle_ctx.input).await
}