pub fn skip() {
    #{ "action": "skip" }
}

pub fn answer(content) {
    #{ "action": "answer", "answer": content }
}


pub async fn main() {
    let particle_inference_ctx = cyb::context.myParticle;
    dbg(cyb::context);
    particle_inference(particle_inference_ctx.input).await
}