import { Container } from 'unstated';
import { getContentByCid, initIpfs } from '../../utils/search/utils';

const APP_NAME = 'cyber';
const SEARCH_RESULT_TIMEOUT_MS = 15000;

const SearchResult = () => {

};

export default SearchResult();
